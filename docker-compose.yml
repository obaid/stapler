services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: stapler
      POSTGRES_PASSWORD: stapler
      POSTGRES_DB: stapler
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
